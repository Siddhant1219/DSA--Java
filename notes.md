--> Patterns (useful to learn for getting good at loops):

** We usually use nested loops **
(1) Outer loop --> number of lines
(2) Inner loop --> columns , establish relation to rows
(3) print required element in the inner loop
(4) Observe symmetry (optional)



---> STL

(1)Pairs

(2)Vectors:

    (i)Ways to insert element into array:
            -v.pushback - have to manually write the pair with curly braces {}
            -v.emplaceback(better option): directly assumes whatever in () to be a pair and inserts in the end
